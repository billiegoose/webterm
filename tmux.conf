# WebTerm tmux config — layered on top of user's config.
# User settings load first; these are WebTerm-specific overrides.
source-file -q ~/.tmux.conf

# Hide status bar (WebTerm has its own toolbar)
set -g status off
